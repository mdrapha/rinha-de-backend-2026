services:
  nginx:
    image: nginx:alpine
    ports:
      - "9999:9999"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api1
      - api2
    deploy:
      resources:
        limits:
          cpus: "0.05"
          memory: "10MB"

  api1:
    build: .
    hostname: api1
    environment:
      - PORT=8080
      - INDEX_PATH=/data/index.bin
    deploy:
      resources:
        limits:
          cpus: "0.475"
          memory: "170MB"

  api2:
    build: .
    hostname: api2
    environment:
      - PORT=8080
      - INDEX_PATH=/data/index.bin
    deploy:
      resources:
        limits:
          cpus: "0.475"
          memory: "170MB"
